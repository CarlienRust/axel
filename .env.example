# Gemini API key — server-side only (Vercel project settings)
# Do NOT prefix with VITE_ — that exposes the key to the browser
GEMINI_API_KEY=

# Optional — defaults to gemini-2.0-flash-lite
# GEMINI_MODEL=gemini-2.0-flash
